apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "spire-agent.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "spire-agent.labels" . | nindent 4 }}
data:
  agent.conf: |
    {{- tpl .Values.config.agentConfTpl . | nindent 4 }}
